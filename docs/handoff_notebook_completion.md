# Handoff — finishing the notebook

**For**: the agent working on the repository locally. **Date**: 2026-09-23.
Everything here is work I could not do from my side because it needs the render trees, which live
under `C:\StabilityMatrix-win-x64\...` and outside the connected folder.

**Read first**: `notebook/AUTHORING.md`. Every rule below is already in it; I am naming the ones
that bite on these particular jobs. Two that decide most of the work:

* **Rule 7.** Every published number comes from a file in `data/`. If a number lives only in
  prose, measure it and persist it *before* it appears in a figure or a page.
* **A figure is generated by a script that reads a measurement file.** A figure that exists on
  disk but cannot be rebuilt is the failure this contract exists to prevent — and it had already
  happened: `F05.1`, `F05.2` and `F05.3` were on disk with a `builder` that was a bare name
  resolving to nothing. They now have real builders in `experiments/notebook_charts.py`.

**State of the figure registry**: 31 figures, **26 builders resolve, 5 do not** —
`F05.4`, `F00.1`, `F00.3`, `F01.1`, `F01.2`. All five are jobs A to C below.

**Run order**: A, B, C, then D. E and F are independent of the rest.

---

## Job A — the two `toggle` builders (`F05.4`, `F00.1`), and `F01.2`

All three are the same shape, so write one builder and call it three times.

**What a toggle is, in this contract.** Two or more images of the same prompt and the same seed,
shown side by side, *plus a control panel of two baselines at different seeds*. The control is
mandatory — it is what tells a reader how much the image moves when nothing was touched. The
registry already declares it: `F05.4` and `F00.1` carry `control: same_baseline_twice`, `F01.2`
carries `control: baseline_pair_seed42_seed777`.

**Signature and house style** — match `experiments/notebook_charts.py`, which already holds
`_canvas`, `_save`, `SURFACE`, `CAT`, `INK`, `DIM`, `GRID`, and note that **the module uses no
numpy**; keep it that way.

```python
def toggle_animation(out: Path, panels: list[tuple[str, str]], control: tuple[str, str],
                     caption_source: Path) -> Path:
    """Whole frames, one prompt, one seed, with a control panel of two untouched baselines.

    `panels` is [(label, image_path), ...] in reading order; `control` is the baseline pair.
    Every label is read out of `caption_source`, never typed here (pitfall 40).
    """
```

**Hard requirements**

* **Whole frames only.** `crop: whole` is what the registry declares. No pixel rectangles — a
  crop in this notebook is a named locator resolved per (prompt, condition, seed), and no locator
  has been qualified yet.
* **Every label read from the measurement file**, not written into the script. `F05.4` reads
  `data/punto7_blocks.csv`, `F00.1` reads `data/bench_checks.csv`. If a label you need is not in
  the file, that is rule 7 telling you to persist it first.
* Dark surface `#1a1a19`, provenance strip under the figure naming the source file and the cell
  count, same as every other figure.
* The output is a **static WEBP**, not an animation, whatever the builder is called. The name
  `toggle_animation` is historical; nothing in the notebook renders motion.

**What each one shows** — the alt text is already registered and is the claim the figure has to
make; do not restate it, satisfy it.

| figure | panels | control | images |
| --- | --- | --- | --- |
| `F00.1` | baseline, the edit, the edit's exact inverse — same prompt, same seed | the same baseline twice | `benchmark_stage1_gate` |
| `F05.4` | block 0 pushed positive, block 27 pushed positive, each against its own baseline at the same seed | the same baseline twice | `benchmark_profondita` |
| `F01.2` | baseline, preset, norm-matched random control — one prompt, one seed | baseline at seed 42 and at seed 777 | `assets/01_steering` (380 files, already in the repo) |

`F00.1` and `F05.4` need the ComfyUI output trees. `F01.2` does not — its images are in the
repository already, which makes it the cheapest of the three and the one to write first.

---

## Job B — `F00.3`, and the number that does not exist yet

The registered alt text says: *"the seed-to-seed noise floor as it was estimated three times:
1.15 percent on three seeds, then 5.2 percent borrowed from other prompts, then 1.65 and 1.84
percent measured on eighteen seeds each."*

`data/bench_checks.csv` holds **only the last two** as structured rows (`noise_floor`, P01 1.65
and P02 1.84, n = 18). The 1.15 and the 5.2 appear nowhere as data — at best inside free-text
notes. **Building the figure from them as it stands would publish two numbers no file supports.**

So Job B is two steps, in this order:

1. **Persist the history.** Write `data/noise_floor_history.csv`, one row per estimate, with at
   minimum: `estimate` (an ordinal), `value_pct`, `n_seeds`, `subject`, `method`, `date`,
   `superseded_by`, and `source` — where `source` names the commit, document or script the figure
   is read out of. If an estimate cannot be traced to a source, write `source: not recorded` and
   say so on the page rather than dropping it: the point of this figure is that the floor moved
   twice before it was measured properly, and an untraceable early estimate is part of that story.
2. **Then** write `noise_floor_history` reading that file, and register it with
   `source: data/noise_floor_history.csv`.

---

## Job C — `F01.1`, and a source file that is not in the repository

`F01.1` is registered with `source: data/stage7_manifest.csv`. **That file does not exist.** This
is the same class of defect as `punto7_manifest.csv`, which page 05 named for a while and which
had never been committed.

Before building: identify what the figure should actually read — the candidates in `data/` are
`stage7a_images.csv` and `stage7b_images.csv` — and correct the registry entry. Do not create an
empty `stage7_manifest.csv` to satisfy the reference.

The figure itself is straightforward: one baseline/preset pair at full resolution, and the same
pair resampled to 224×224, the size CLIP looks at. It is the evidence for the
`clip-224-is-blind-to-it` claim, so the downsample must be done in the builder, from the full-size
files, and not by pasting a thumbnail someone made earlier.

---

## Job D — page 01, `01-mark-style`

The largest missing piece. It is the only page that has never been written, and it holds **six of
the nine claims still pending** in `notebook/_migrating.yaml`.

**Material**: `assets/01_steering` (380 files) and `assets/01_steering_stage7` (608). The page is
not blocked for lack of data.

**The six claims it owns**, verbatim from the ledger — each needs its own anchor and its own
evidence line, and each number in that evidence line must come from a file in `data/`:

1. `tiny-payload-shifts-mark-style` — a 53 KB hand-calibrated perturbation shifts mark style
   coherently and repeatably across seeds on a 12.8-billion-parameter model.
2. `direction-not-distance` — controls at the same Frobenius displacement, without the calibrated
   structure, do not reproduce it.
3. `clip-224-is-blind-to-it` — CLIP at 224×224 cannot see the change; measured on mark morphology
   the effect is solid.
4. `mark-style-generalises` — on 24 prompts, against a criterion declared in advance.
5. `colour-does-not-generalise` — present only where the prompt pins the palette, absent when the
   model chooses it.
6. `preset-is-a-sharp-operator` — darker, greyer, grainier, parallel-stroked, headlights on. Five
   effects, one direction, two unrelated corpora.

**Before writing a word of it, settle two things.**

* **Status and stage, per claim.** The validator rejects `holds` with `exploratory`. Claims 4 and
  6 speak of a criterion declared in advance and of two corpora; if a pre-registration exists,
  name it in the front matter and the file must be in the repository — the validator checks that
  now. If no pre-registration exists, the stage is `exploratory` and no claim on the page may be
  `holds`. **Do not pick the status that makes the page look better; pick the one the documents
  support, and if they are silent, `open` is the honest answer.**
* **Where `operative-structural-property-unknown` lives.** The ledger leaves it between page 01
  and page 02 and says to decide before publishing it anywhere. Decide, then publish it once.

**Structure** — identical to every other page, in this order:

1. Front matter: `id`, `title`, `status`, `stage`, `date`, `preregistration`, `supersedes`,
   `pitfalls`, `corpus`, `claims`.
2. The status blockquote. **Its render count must equal `corpus.renders`** — the validator checks
   this, after three pages were caught disagreeing with themselves.
3. **The opening block**, three lines, in Alessandro's first person, with these exact lead-ins and
   in this order: `**The direction I'm chasing.**`, `**What would kill it.**`, `**Where we are.**`
   The validator errors if one is missing or out of order. The third line says the uncomfortable
   part; if it cannot, the reservations section is probably thin.
4. `## In two minutes`, `## The verdict`, `## Why I might be wrong`, `## The data` (with
   `### Reproducing this`), `## Provenance`.
5. The reproducibility block: print it with `python experiments/extract_repro.py 01-mark-style`
   and copy what it prints. Do not type it. What no file records, write as `not recorded` naming
   what *would* record it — never a plausible value.
6. A `* Renders:` line in Provenance itemising each bench with its count, so the validator can
   check the total.

**When the page exists**, move its six claim ids from `pending` to `migrated` in
`notebook/_migrating.yaml`, keeping the arithmetic invariant documented at the top of that file.

---

## Job E — the scope page

Two pending claims belong to the notebook's front matter and not to any experiment:
`bounded-to-one-model-one-corner` (all 40 prompts are upper-body or close-up portraits and all 40
contain "bold ink outlines" and "hatched shadows") and `exploratory-sizes-are-upper-bounds` (three
consecutive confirmation rounds came back between a third and a half of the exploratory estimate).

The first is the limit that bounds every result in the notebook. It should be visible before a
reader reaches any page, not after.

---

## Job F — a page for `all_blocks_clean_v2`

Decided as a separate experiment. 294 HUD-free renders, six block groups, three angles, both
signs, 2 prompts × 3 seeds. `stage: exploratory`, no pre-registration, therefore **no claim on it
may be `holds`** — `open` throughout.

Data already computed: `data/all_blocks_clean_v2_response_by_block.csv`,
`..._separability.csv`, `data/all_blocks_specialization_summary.csv`,
`data/specialisation_vs_proximity.csv`.

Two results it can carry, and they disagree with each other, which is the interesting part:

* Response grows monotonically with angle for every block, and `B6` runs 0.63 / 1.65 / 3.71
  against `B1`'s 0.12 / 0.40 / 0.90 and the middle groups' 0.07 to 0.49.
* The chroma/shape ratio makes `B1` and `B6` the closest of all fifteen block pairs (gap 0.0073,
  permutation p over pairs 0.067) — *same job, different depth*. But the separability test puts
  `B1`–`B6`, `B1`–`B2` and `B1`–`B4` all at the exact floor with similar cosines, which says `B1`
  is unlike several groups rather than paired with `B6`.

Write both. A page that reports two statistics pointing different ways on one corpus is doing its
job; one that picks the tidier is not.

---

## Verification, every time

```
python experiments/notebook_charts.py          # rebuild every registered chart
python experiments/validate_notebook.py        # must end PASS -- 0 error(s)
python experiments/test_validate_notebook.py   # must end 26/26
python experiments/build_notebook.py           # regenerates README.md and index.html
```

Never hand-edit `README.md` or `index.html`. If a frozen analysis script has to run against
different inputs, derive a copy that changes only its path constants, prove the diff and log both
sha256s — and check **every** output constant, not the ones you noticed: a copy of
`analyze_block1_vs_block6.py` that redirected two of its four paths overwrote the historical
results file on 2026-09-23.
